// Contenu du portfolio, centralisé dans src/data/site.json.
// Modifie ce JSON (à la main ou via /admin/) pour mettre à jour les pages Accueil / Projets / Recherche / CV.

import site from "./data/site.json";

export const contactInfo = site.contactInfo;
export const skills = site.skills;
export const projects = site.projects;
export const publications = site.publications;
export const research = site.research;
export const education = site.education;
export const experience = site.experience;
